APPLICATION_NAME=${project.artifactId}
IMAGE_DOCKER_NAME=registry.tools.orange-sonatel.com/dif/${project.artifactId}
PORT_APPLICATION_NUMBER=${server.port.docker.expose}
DB_SERVICE_NAME=${project.artifactId}-db
DB_NAME=selfcare_b2c_account_management
DB_TYPE=mysql
DB_USER=root
DB_PASSWORD=digitalfactory
DB_PORT=3305
PROFILE_ACTIVE=dev
IMAGE_DOCKER_TAG=${env.VERSION}.b${env.BUILD_NUMBER}
CPU=500
MEMORY=500
READINESS_DELAY=200
LIVENESS_DELAY=200
